"""Risk-engine wrapper around SessionEngine.new_position_decision (TFEX-1), so that.

Not implemented — **Milestone TFEX-4**.

Risk-engine wrapper around SessionEngine.new_position_decision (TFEX-1), so that
every rejection reason lands in one audit record.
"""

from __future__ import annotations

__all__: list[str] = []
