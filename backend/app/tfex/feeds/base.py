"""Feed protocol shared by replay and real-time sources, so that replay and.

Not implemented — **Milestone TFEX-2**.

Feed protocol shared by replay and real-time sources, so that replay and
real-time semantics can be compared directly (CLAUDE.md: replay == realtime).
"""

from __future__ import annotations

__all__: list[str] = []
