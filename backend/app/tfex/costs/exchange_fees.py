"""Exchange fees. The contract summary caps this at THB 7 per contract per side;.

Not implemented — **Milestone TFEX-5**.

Exchange fees. The contract summary caps this at THB 7 per contract per side;
that value is configuration (``contract.max_exchange_fee_thb_per_contract_per_side``)
and must be verified against the current schedule before live use.
"""

from __future__ import annotations

__all__: list[str] = []
